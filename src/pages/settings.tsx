export default function Settings() {
  return (
    <>
      <button
        type="button"
        className="text-white"
        onClick={() => console.log("hello")}
      >
        Click Me!
      </button>
    </>
  );
}
